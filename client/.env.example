# Base URL of the backend API. In local dev this points at the Express server.
# In production on Render, set this to your server service's URL, e.g.
# https://audit-doc-analyzer-server.onrender.com
VITE_API_BASE_URL=http://localhost:3001
