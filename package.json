{
  "name": "audit-doc-analyzer",
  "version": "1.0.0",
  "private": true,
  "description": "Audit document analysis tool: upload agreements/SOPs, extract text, get AI-assisted synopses and process walkthroughs.",
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "install:all": "npm install --prefix server && npm install --prefix client",
    "build:client": "npm run build --prefix client"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
